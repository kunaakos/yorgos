{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "target": "esnext",
        "moduleResolution": "node",
        "esModuleInterop": true, // NOTE: this is needed because of jest only, this package has no deps or non-es modules
        "paths": {
            "src/*": ["./src/*"]
        }
    },
    "exclude": ["dist/**/*"]
}
